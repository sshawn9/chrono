precice_adapter_config:
    data_path:
      type: RELATIVE
      root: "."

    participant_name: Solver2
    precice_config_filename: test.xml
    
    interfaces:
    - mesh_name: Mesh2
      write_data:
      - name: data2
      read_data:
      - name: data1
